Greetings,


Thank you for contacting AWS Support Engineering. I'm Rodrigo from Networking team, and I'll be your dedicated support engineer for this case. I want to assure you that I'll be personally overseeing your inquiry.


Based on your case description, I understand that the ARC Region Switch workflow for plan `spread-aurora-regionswitch` consistently succeeds when turning a routing control On, but fails at the step that should turn the opposite control Off, resulting in both routing controls ending up in an On state.


Please confirm if this accurately represents your inquiry.



I reviewed the CloudTrail events in account 970688031697 (us-west-2) and here are my findings:


1. I identified 4 `StartPlanExecution` events between 13:32 and 15:33 UTC on May 19th, corresponding to plan versions 4, 5, 7, and 8 respectively. None of the executions completed the Off step successfully.


2. The only `UpdateRoutingControlStates` event generated by the workflow (role `RegionSwitchExecutionRole-2dcb89383d59`, invoked by `arc-region-switch.amazonaws.com`) was at 13:41:55Z, and it corresponds to setting `Primary-us-east-1` to On — a redundant operation since it was already in that state.


3. There is no CloudTrail event showing the workflow attempting an Off operation on any routing control. This indicates the workflow failed internally before reaching the Off step, or the step did not translate into an API call.


4. I confirmed that the manual Off operation (performed by user `O780982` at 14:18:25Z via the console) was successful. This rules out issues with the routing control itself, insufficient permissions at the control level, or safety rules blocking the operation.


5. I verified the control panel configuration (`030b84cff497421a8c61d0159505b49c`): both routing controls (`Primary-us-east-1` and `Standby-us-west-2`) are in DEPLOYED status, and no safety rules are configured on the control panel.



Based on this evidence, the routing controls and the ARC data plane are functioning correctly. The issue resides within the Region Switch workflow engine — the Off step is not being executed.




Recommendations:


- As an immediate workaround, you can execute the failover manually via CLI using a single batch call, which ensures atomicity and avoids the intermediate state where both controls are On:

```
aws route53-recovery-cluster update-routing-control-states \
  --update-routing-control-state-entries \
  '[{"RoutingControlArn":"arn:aws:route53-recovery-control::970688031697:controlpanel/030b84cff497421a8c61d0159505b49c/routingcontrol/2c81243a799e4179","RoutingControlState":"On"},{"RoutingControlArn":"arn:aws:route53-recovery-control::970688031697:controlpanel/030b84cff497421a8c61d0159505b49c/routingcontrol/14245b6c602649c5","RoutingControlState":"Off"}]' \
  --region us-west-2 \
  --endpoint-url "<your-cluster-endpoint>"
```


- I recommend adding an assertion safety rule ("at least 1 routing control must be On") to your control panel to protect against split-traffic scenarios.


- I suggest documenting a manual failover runbook as backup for incident response while the automated workflow issue is being resolved.



To continue the investigation, I need the following additional information:



1. The complete plan definition:



```
aws arc-region-switch get-plan \
  --plan-arn arn:aws:arc-region-switch::970688031697:plan/spread-aurora-regionswitch:qmc43o \
  --region us-west-2
  
```

I need to review the steps defined, their types, the ARNs referenced, and the execution order.


2. The detail of the first failed execution:


```
aws arc-region-switch get-plan-execution \
  --plan-arn arn:aws:arc-region-switch::970688031697:plan/spread-aurora-regionswitch:qmc43o \
  --execution-id us-west-2/2f8fdf236d3541c1 \
  --region us-west-2
```

I need to identify which specific step failed, the error message, and whether the workflow reached the Off step at all.



3. The IAM policy for the execution role:

```
aws iam list-attached-role-policies --role-name RegionSwitchExecutionRole-2dcb89383d59
```

I need to confirm the role allows `UpdateRoutingControlState` without condition keys restricting it to only the On state.


4. What modifications were made between plan versions 4 and 8? Screenshots of the current step configuration in the ARC Region Switch console would also be helpful.



Please review our comprehensive documentation and the information shared above.  Should you need any assistance during implementation or require further clarification, please don't hesitate to reach out. Additionally, if you would like to schedule a Chime meeting, please suggest a convenient time, and I will adjust my calendar accordingly.
――――――――――――――― References ―――――――――――――――


[1] ARC Routing Controls - https://docs.aws.amazon.com/r53recovery/latest/dg/routing-control.html


[2] UpdateRoutingControlStates API - https://docs.aws.amazon.com/routing-control/latest/APIReference/API_UpdateRoutingControlStates.html


[3] Safety Rules - https://docs.aws.amazon.com/r53recovery/latest/dg/routing-control.safety-rules.html


[4] ARC Region Switch - https://docs.aws.amazon.com/r53recovery/latest/dg/arc-region-switch.html
